import 'package:flutter/material.dart';

class Ezlink extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        child: Center(
          child: Text('Ezlink display page',
            style: TextStyle(fontSize: 25, fontWeight: FontWeight.bold)
            ),
        ),
      ),
    );
  }
}